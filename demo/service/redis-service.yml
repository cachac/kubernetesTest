apiVersion: v1
kind: Service
metadata:
  name: redis-service
  labels:
    name: redis-service
    app: demo-voting-app

spec:
  ports:
    - port: 6379 # service port
      targetPort: 6379

  selector: # labels of pod
    name: redis-pod
    app: demo-voting-app 

